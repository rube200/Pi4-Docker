    location ~* /robots\.txt$ {
        alias /usr/share/nginx/html/robots.txt;

        access_log off;
        log_not_found off;
    }

    location ~* /favicon\.ico$ {
        alias /usr/share/nginx/html/favicon.ico;

        access_log off;
        log_not_found off;
    }
