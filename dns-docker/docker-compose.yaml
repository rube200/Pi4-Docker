services:
  unbound:
    build: ./unbound-docker/
    container_name: unbound
    ports:
      - "5335:5335/udp"
      - "5335:5335/tcp"
    privileged: true
    networks:
      - dns_network
    restart: unless-stopped

networks:
  dns_network:
    driver: bridge
