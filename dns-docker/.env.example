# DOH Server Configuration
# Required: Set your DoH server hostname
DOH_HOSTNAME=your-hostname.com

# Optional: Override defaults if needed
# DOH_UPSTREAM_DNS=pihole:53
# DOH_PATH_PREFIX=query-dns
# DOH_PUBLIC_PORT=443